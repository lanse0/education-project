server:
  port: 8080
spring:
  application:
    name: stu-server
  cloud:
    nacos:
      server-addr: @nacos-server@
  profiles:
    active:
      - @runtime-profile@
      #表示导入一个后缀为log的yml文件 application-log.yml
      #- log
      #- datasource
