<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.qf.business.student.dao.UserDao">

    <resultMap type="com.qf.data.student.entity.User" id="UserMap">
        <result property="id" column="id" jdbcType="INTEGER"/>
        <result property="username" column="username" jdbcType="VARCHAR"/>
        <result property="password" column="password" jdbcType="VARCHAR"/>
        <result property="name" column="name" jdbcType="VARCHAR"/>
        <result property="sex" column="sex" jdbcType="INTEGER"/>
        <result property="header" column="header" jdbcType="VARCHAR"/>
        <result property="role" column="role" jdbcType="INTEGER"/>
        <result property="createTime" column="create_time" jdbcType="TIMESTAMP"/>
        <result property="updateTime" column="update_time" jdbcType="TIMESTAMP"/>
        <result property="status" column="status" jdbcType="INTEGER"/>
        <result property="delFlag" column="del_flag" jdbcType="INTEGER"/>
    </resultMap>
    <resultMap id="userAndCourseResultMap" extends="UserMap" type="com.qf.data.student.entity.User">
        <collection property="courses" ofType="com.qf.data.student.entity.Course">
            <id property="id" column="cid"></id>
            <result property="courseName" column="course_name"></result>
        </collection>
    </resultMap>

    <!-- 批量插入 -->
    <insert id="insertBatch" keyProperty="id" useGeneratedKeys="true">
        insert into course.user(username, password, name, sex, header, role, create_time, update_time, status, del_flag)
        values
        <foreach collection="entities" item="entity" separator=",">
            (#{entity.username}, #{entity.password}, #{entity.name}, #{entity.sex}, #{entity.header}, #{entity.role},
            #{entity.createTime}, #{entity.updateTime}, #{entity.status}, #{entity.delFlag})
        </foreach>
    </insert>
    <!-- 批量插入或按主键更新 -->
    <insert id="insertOrUpdateBatch" keyProperty="id" useGeneratedKeys="true">
        insert into course.user(username, password, name, sex, header, role, create_time, update_time, status, del_flag)
        values
        <foreach collection="entities" item="entity" separator=",">
            (#{entity.username}, #{entity.password}, #{entity.name}, #{entity.sex}, #{entity.header}, #{entity.role},
            #{entity.createTime}, #{entity.updateTime}, #{entity.status}, #{entity.delFlag})
        </foreach>
        on duplicate key update
        username = values(username) , password = values(password) , name = values(name) , sex = values(sex) , header =
        values(header) , role = values(role) , create_time = values(create_time) , update_time = values(update_time) ,
        status = values(status) , del_flag = values(del_flag)
    </insert>
    <!--     SELECT
                 u.*,c.*,c.id cid
             FROM
                 user u
             JOIN student_course sc ON u.id = sc.sid
             JOIN course c ON sc.cid = c.id where u.role = 2

             解决分页插件关联嵌套查询结果不正确问题 -->
    <select id="queryUserAndCourse" resultMap="userAndCourseResultMap">
        SELECT
            u.*,c.*,c.id cid
        FROM
            (select * from user where role = 2 limit ?) u
        JOIN student_course sc ON u.id = sc.sid
        JOIN course c ON sc.cid = c.id
    </select>

</mapper>

